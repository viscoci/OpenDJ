// OpenDJ OSS demo — placeholder entrypoint.
//
// When @opendj/backend ships, this file should:
//   1. Import createApp from '@opendj/backend'
//   2. Build runtime config from process.env
//   3. Construct deps via createDeps(config)
//   4. Boot the Hono app on a Node http server bound to 0.0.0.0:8888
//   5. Register the in-process NodeSessionRoom for realtime
//   6. Start the slot expiry sweep + token refresh interval
//
// See docs/agent-brief.md §"OSS deploy story" for the full target experience.

console.log('OpenDJ OSS demo — scaffold only. Backend implementation pending.');
console.log('See docs/agent-brief.md for the full architecture.');
