# Onboarding (self-host)

> **Status:** placeholder. Real onboarding lands once `apps/oss-demo` boots end-to-end.

This doc will walk you through:

1. Cloning the repo and installing dependencies (`pnpm install`)
2. Setting up a Spotify Developer Dashboard application
3. Configuring `apps/oss-demo/.env`
4. Running `docker compose up`
5. Opening `http://localhost:8888`, logging in with Spotify, and sharing the `/queue` URL with guests

Until then:

- [`README.md`](../README.md) — repo overview + quickstart
- [`docs/agent-brief.md`](./agent-brief.md) — full architecture spec
- [`ROADMAP.md`](../ROADMAP.md) — what's landed vs in-flight
